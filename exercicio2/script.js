const numeros = [1, 4, 9, 16, 25, 36, 49, 64]
console.log(numeros)

const triplo = numeros.map(item => item * 3)
console.log(triplo)

const metades = numeros.map(item => item / 2)
console.log(metades)
